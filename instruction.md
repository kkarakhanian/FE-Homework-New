MVC + Constructor Functions
[View] — listens to events → calls → [Controller]
[Controller] — processes data → transfers to → [Model]
[Model] — creates/changes data → stores in localStorage → notifies → [View]
[View] — renders HTML
